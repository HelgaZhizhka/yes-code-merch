{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '\n📋 Session end checklist:\n  1. Update claude-progress.md (what was done, next step)\n  2. Update feature_list.json (mark done if DoD met)\n  3. Commit clean state\n  4. ./init.sh must pass\n'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE 'git commit|git push'; then echo '⚠️  Pre-commit: run ./init.sh first if not done yet'; fi"
          }
        ]
      }
    ]
  }
}
